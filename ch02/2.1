2.1-1
略


2.1-2
INSERTION-SORT-DESCENDING ORDER(A)
for j=2 to A.length
    key=A[j]
    i=j-1
    while i>0 and key>A[i]
        A[i+1]=A[i]
        i=i-1
    A[i+1]=key


2.1-3
LINEAR SEARCH(A,v)
for i=1 to A.length
    if v=A[i]
       return i
return NIL

初始化:for 循环前是A
保持:for后仍是A
终止:i=A.length+1,A仍为A,得出v是否在内


2.1-4
C = Array[A.length+1]
carry <- 0,
for i <- A.length to 1
    C[i+1] <- (A[i] + B[i] + carry) % 2;
    carry = (A[i] + B[i] + carry)/2;
C[1] <- carry;
return C
思考时想到的是由开始的情况来解决问题
别人的答案是从结果的情况解决问题通过对结果数据的处理
我实属弟弟
